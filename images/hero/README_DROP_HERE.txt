DROP HERE — Cover / Hero Images

Save the 3 Google Street View screenshots you sent into THIS folder with these EXACT filenames:

  1. 01_storefront_wide.jpg       ← The wide street view showing Augustine's at center
  2. 02_storefront_closeup.jpg    ← The close-up of the navy doors with "Augustine's Salumeria" sign
  3. 03_storefront_context.jpg    ← The side angle showing adjacent buildings + brick context

The site will automatically use:
  • #1 as the password gate hero background (full-bleed left panel)
  • #1, #2, #3 as the featured trio in the Property section "Cover Images" gallery
  • #2 (close-up) as the inline property image on the right of the Location/Demographics block

Once the files are in this folder, the site will pick them up automatically — no code changes needed.

If you save them as JPEG (.jpg), the site will load them. If you save them as PNG (.png), edit
the file extension references in index.html (search for "/hero/01_storefront_wide.jpg").

Recommended dimensions:
  • Wide format (16:9 or 4:3) for the gate hero, at least 1600px wide
  • The site will lazy-load and the lightbox will display them at full resolution

